# Supported Features

Here is the complete feature matrix for LarkBatis.

- :material-check: **Supported**: Implemented, optimized, and fully tested.
- :material-alert: **Narrowed**: Supported with specific compile-time constraints.
- :material-close: **Dropped**: Omitted by design to eliminate reflection and runtime interpretation. Produces a clear compile error naming the alternative.

Version **`0.1.0`**.

## Statements

| Feature | Status | Notes |
|---|---|---|
| `@Select` `@Insert` `@Update` `@Delete` | :material-check: | Accepts `String[]` (lines joined with a single space) |
| `<select>` `<insert>` `<update>` `<delete>` in mapper XML | :material-check: | Namespace matches interface FQN; `id` matches method name. [Details](../usage/xml-mappers.md) |
| Mixing annotations and XML in one mapper | :material-check: | Resolved per method; having both or neither is a compile error |
| `#{}` bind parameters | :material-check: | Resolved against method parameter types at compile time |
| `${}` raw splices | :material-alert: | Strictly restricted to `SqlFragment`, closed-value types, or `@OrderBy`. [Details](../usage/raw-sql.md) |
| `@Param` annotations | :material-check: | Explicit parameter naming |
| `@Options(useGeneratedKeys, keyProperty, keyColumn)` | :material-check: | [Details](../usage/generated-keys.md) |
| `<selectKey>` | :material-close: | Write sequence lookup as an explicit query |
| `@SelectProvider` / `@InsertProvider` | :material-close: | Runtime SQL generation methods are invisible to the compiler. Use XML or the escape hatch |
| Untyped `Map` or `Object` parameters | :material-close: | Compiler requires concrete types to generate parameter setters. Use typed objects or `@Param` |
| `RowBounds` in-memory paging | :material-close: | Page in SQL using `LIMIT` and `OFFSET` parameters |

## Dynamic SQL

| Feature | Status | Notes |
|---|---|---|
| `<if>` | :material-check: | Compiled to boolean locals and evaluated once |
| `<choose>` / `<when>` / `<otherwise>` | :material-check: | Mutual exclusion baked into generated bytecode |
| `<where>` / `<set>` | :material-check: | Constant-folded without runtime string scans |
| `<trim>` | :material-alert: | Literal attributes only, folded at compile time |
| `<sql>` / `<include>` | :material-alert: | Static `refid` only, inlined at compile time |
| `<foreach>` | :material-alert: | Statically-typed collections/arrays/maps. Throws on empty collections. [Details](../usage/foreach-and-batches.md) |
| `@PadPow2` | :material-check: | Bounds dynamic `IN` clause SQL variants to power-of-two lengths |
| `<bind>` | :material-close: | Compute values in Java and pass as parameters |
| Full OGNL in `test` | :material-close: | Replaced by a [type-checked test grammar](../usage/dynamic-sql.md#the-test-grammar) |
| `databaseId` attribute | :material-close: | Use dedicated mapper interfaces per database vendor |

## Results & Mapping

| Feature | Status | Notes |
|---|---|---|
| `resultType` convention mapping | :material-check: | `snake_case` → `camelCase` applied at compile time |
| Positional column reads | :material-check: | Hardcoded column indexes for parsed select lists |
| Name-based metadata fallback | :material-check: | Used for `SELECT *` or dynamic splices; resolved once from metadata |
| Scalar returns | :material-check: | `long`, `String`, etc. read column 1 directly without bean allocation |
| `List<T>` returns | :material-check: | Standard collection return |
| `Stream<T>` returns | :material-check: | Stream rows over open cursor; caller closes. [Details](../usage/streaming.md) |
| `<resultMap>` | :material-alert: | Explicit column-to-property mapping (no implicit guessing) |
| `<association>` / `<collection>` | :material-alert: | Single-level nesting via SQL join; query must be ordered by parent ID |
| Nested `select=` in result maps | :material-close: | Causes N+1 queries. Use SQL joins instead |
| `<discriminator>` | :material-close: | Use separate queries with distinct return types |
| `<constructor>` results | :material-close: | Provide standard no-arg constructor and setters |
| Result map `extends`, `columnPrefix`, `autoMapping` | :material-close: | Declare mappings explicitly or use `resultType` |
| Lazy loading | :material-close: | Requires dynamic proxy per object. Use joins or explicit second query |
| Type aliases | :material-close: | Use fully-qualified class names |

## Types & Handlers

| Feature | Status | Notes |
|---|---|---|
| Primitives and boxed wrappers | :material-check: | Boxed wrappers use `JdbcCodec` for proper null handling |
| `String`, `BigDecimal`, `BigInteger`, `byte[]` | :material-check: | Built-in direct JDBC mapping |
| `java.sql.Date`, `Time`, `Timestamp` | :material-check: | Built-in direct JDBC mapping |
| `Instant`, `LocalDate`, `LocalTime`, `LocalDateTime` | :material-check: | Built-in conversion via `JdbcCodec` |
| Enums (by name) | :material-check: | Built-in mapping; enums are closed-value types safe for `${}` |
| `@Column` | :material-check: | Explicit column name on field, setter, or getter. [Details](annotations.md#column) |
| `@Handler` custom handlers | :material-check: | Declared on properties, parameters, or in mapper XML. [Details](../usage/types.md#custom-type-handlers) |
| Global type handlers (`-Alarkbatis.typeHandlers`) | :material-check: | Configured per Java type at compile time. [Details](configuration.md#type-handlers-for-a-whole-build) |
| Runtime TypeHandler discovery | :material-close: | Replaced by explicit compile-time configuration |

## Sessions, Transactions & Execution

| Feature | Status | Notes |
|---|---|---|
| `LarkBatisTx` scopes & vote-to-commit | :material-check: | Standalone transaction manager. [Details](../usage/transactions.md) |
| Spring `@Transactional` | :material-check: | Automatic integration via `DataSourceUtils` |
| Spring exception translation | :material-check: | Translates JDBC exceptions to Spring's `DataAccessException` hierarchy |
| Spring Boot auto-configuration | :material-check: | Single starter compatible with Spring Boot 3 and Boot 4 |
| Generated Spring `@Configuration` | :material-check: | Registers mappers as beans with `proxyBeanMethods = false` |
| Batch inserts | :material-check: | Declared via method signature (`List<T>` parameter) |
| Multi-row `VALUES` via `<foreach>` | :material-check: | Generates multi-row insert statements |
| Escape hatch (`query`, `queryOne`, `queryStream`, `update`) | :material-check: | Executes dynamic SQL safely via `SqlFragment` |
| `ExecutorType.BATCH` / `REUSE` | :material-close: | Replaced by batch method signatures |
| Interceptor plugins | :material-close: | Replaced with explicit SQL, type handlers, or Spring AOP. [See recipes](mybatis-differences.md#what-replaces-a-plugin) |
| Second-level cache (`<cache>`, `<cache-ref>`) | :material-close: | Cache at the service level (e.g. Spring Cache, Redis) |
| First-level cache | :material-close: | Connections are returned immediately to the pool |
| Runtime `addMapper()` | :material-close: | Mapper registry is closed at compile time |
| Multiple `DataSource`s per mapper | :material-alert: | Configure one session per `DataSource` and declare `@Bean` methods manually |
| Dynamic SQL logging (`log-sql`) | :material-close: | Configured at the connection pool or driver level (`datasource-proxy`, p6spy) |

## Build & Tooling

| Feature | Status | Notes |
|---|---|---|
| Annotation processor (javac) | :material-check: | Standard javac compiler (ECJ is not supported) |
| Gradle plugin | :material-check: | [Details](../getting-started/build-plugins.md) |
| Maven plugin | :material-check: | Requires `<extensions>true</extensions>` |
| JPMS named modules | :material-check: | Real named modules for all published artifacts |
| Lombok compatibility | :material-check: | Declare LarkBatis processor *after* Lombok in build file |
| Incremental compilation | :material-alert: | Compile with `-parameters` flag for reliable incremental builds |
| Test-scoped mappers | :material-close: | Mappers belong in main compilation source sets |
| GraalVM native image | :material-alert: | Zero runtime reflection by design (smoke test pending in M5) |
| Migration scanner | :material-check: | `larkbatis-scan` CLI tool. [Details](migration.md) |

## Related Documentation

- [MyBatis Differences](mybatis-differences.md): Detailed explanations for dropped and narrowed features
- [Annotations](annotations.md): Full reference for all LarkBatis annotations
- [Runtime API](runtime-api.md): Public runtime API surface
- [Configuration](configuration.md): Compiler options and application properties
